version: 1
backend:
  phases:
    preBuild:
      commands:
        - npm install
    build:
      commands:
        - echo "No build step required for Express backend"
  artifacts:
    baseDirectory: /
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*

