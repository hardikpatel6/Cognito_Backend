version: 1.0
frontend:
  phases:
    pre_build:
      commands:
        - npm install
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: 'build'
    files:
      - '**/*'
  cache:
    paths:
      - 'node_modules/**/*'